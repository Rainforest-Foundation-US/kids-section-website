import { HomeGoNextSectionButton, NavBarButton } from "@/components/buttons";
import { HomeSection, HomeSectionsContainer } from "@/components/sections";
import Head from "next/head";
import Image from "next/image";

export default function Home() {
  return (
    <>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <main className="snap-mandatory snap-y h-screen overflow-y-auto bg-neutral-600">
        <HomeSectionsContainer>
          <HomeSection className="h-screen snap-center flex flex-col relative">
            <header className="p-6 w-full z-10 border-b-1 border-neutral-500 flex flex-row justify-between">
              <Image
                className="h-[66px] w-[262px]"
                src="/large-logo.png"
                height={66 * 2}
                width={262 * 2}
                alt="Rainforest Logo"
              />

              <div className="flex flex-row space-x-2 items-center">
                <NavBarButton>Home</NavBarButton>
                <NavBarButton>About the Amazon</NavBarButton>
                <NavBarButton>Narratives</NavBarButton>
                <NavBarButton>Q&A</NavBarButton>
              </div>
            </header>

            <Image
              className="object-cover block absolute inset-0 w-full h-full"
              src="/sections/welcome/background.png"
              height={1280}
              width={720}
              aria-hidden
              alt=""
            />

            <div className="z-10">
              <HomeGoNextSectionButton />
            </div>
          </HomeSection>

          <HomeSection className="h-screen snap-center flex bg-complementary-600 relative">
            <div className="z-10">
              <HomeGoNextSectionButton />
            </div>
          </HomeSection>

          <HomeSection className="h-screen snap-center bg-error-700 flex relative">
            <div className="z-10">
              <HomeGoNextSectionButton />
            </div>
          </HomeSection>
        </HomeSectionsContainer>
      </main>
    </>
  );
}
